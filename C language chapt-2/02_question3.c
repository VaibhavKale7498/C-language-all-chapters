#include<stdio.h>

int main(){
     // write a c program to determine weather a number is divisibl by 97 or not
     int num;
     printf("Enter the number\n");
     scanf("%d",&num);
     printf("Divisibility test return: %d",num/97);
     return 0;
}